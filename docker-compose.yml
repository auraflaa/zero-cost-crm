# Build and run the production application locally or on a server.
#
# Usage:
#   docker compose build
#   docker compose up -d

services:
  crm:
    build:
      context: .
      dockerfile: Dockerfile
    image: convobrains-crm:local
    pull_policy: build
    container_name: convobrains-crm
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '4000:4000'
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    networks:
      - default
      - functional_default
    environment:
      NODE_ENV: production
      PORT: 4000
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:4000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

networks:
  default:
  functional_default:
    name: functional_default
    external: true
